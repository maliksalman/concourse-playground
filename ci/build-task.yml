---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: {{DOCKER_IMAGE}}
    tag: latess
    
inputs:
- name: pipeline
- name: ftp-api

run:
  path: bash
  args: [ pipeline/ci/build-task.sh ]
